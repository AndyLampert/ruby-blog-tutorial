<!-- 
For new.html.erb
html is the format of the template
erb is the handler that will be used
-->

<%= form_for :article, url: articles_path do |f| %>
	<p>
		<%= f.label :title %><br>
		<%= f.text_field :title %>
	</p>
	<p>
		<%= f.label :text %><br>
		<%= f.text_area :text %>
	</p>
	<p>
		<%= f.submit %>
	</p>
<% end %>

<!-- check to see how the route, controller, action and view are working -->
